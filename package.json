{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Expense Tracker using MEAN stack - Fully configured for Vercel deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && ng serve",
    "build": "cd client && npm install && npm run build -- --configuration production",
    "vercel-build": "cd client && npm install && npm run build -- --configuration production",
    "seed": "node seed.js"
  },
  "keywords": [
    "expense",
    "tracker",
    "MEAN",
    "MongoDB",
    "Express",
    "Angular",
    "Node",
    "Vercel"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.0"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
