{
  "version": 2,
  "buildCommand": "cd client && npm install --legacy-peer-deps && npm run build -- --configuration production",
  "outputDirectory": "client/dist/expense-tracker-client",
  "installCommand": "npm install && cd client && npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

